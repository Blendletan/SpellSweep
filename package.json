{
  "name": "spellsweep",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test src/game.test.ts src/dictionary.test.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
